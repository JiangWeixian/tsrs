{
  "name": "ts-swc-mono",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.3.0",
  "description": "",
  "keywords": ["typescript", "swc"],
  "license": "MIT",
  "author": "JW",
  "scripts": {
    "test": "echo \"TODO:\"",
    "artifacts": "pnpm --filter=./crates/binding_node run artifacts",
    "ci:version": "pnpm changeset version",
    "ci:publish": "pnpm changeset publish",
    "ci:snapshot": "pnpm changeset version --snapshot snapshot",
    "ci:prerelease": "pnpm changeset publish --no-git-tag --snapshot",
    "lint:fix": "eslint . --fix",
    "build:all": "run-s build:*",
    "build:rust": "cargo build",
    "build": "pnpm --filter=./crates/binding_node run build",
    "prepare": "husky install"
  },
  "lint-staged": {
    "**/**/*.{js,ts,tsx,json,yml,yaml,md}": ["eslint --fix"]
  },
  "devDependencies": {
    "@aiou/eslint-config": "1.5.0",
    "@changesets/cli": "^2.26.0",
    "cz-emoji": "^1.3.1",
    "eslint": "^8.56.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.1.0",
    "npm-run-all": "4.1.5",
    "typescript": "^4.6.4"
  },
  "publishConfig": {
    "access": "restricted"
  }
}
